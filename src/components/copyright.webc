<script webc:type="js" webc:is="template">
	const now = new Date();
	let author = site.author.name;

	if (site.author.url) {
		author = `<a href="${site.author.url}">${author}</a>`;
	}

	`&copy; ${now.getFullYear()} ${author}.`;
</script>
